name: test
channels:
  - conda-forge
  - dglteam

dependencies:
  - python
  - pip

  # testing
  - pytest
  - pytest-cov

  # core deps
  - nagl
  - openff-toolkit
  - qubekit
  - pytorch-lightning
  - gdl >=0.7